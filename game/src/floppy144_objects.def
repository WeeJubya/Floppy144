/*
 * Master reconstructed-object list
 *
 * This file intentionally has no include guard. It is an X-macro definition
 * file used to generate stable IDs and immutable scene-object metadata.
 *
 * Arguments:
 *   symbol
 *   semantic name
 *   owning scene
 *   parent object
 *   local x
 *   local y
 *   draw layer
 *   collision x
 *   collision y
 *   collision width
 *   collision height
 *   interaction x
 *   interaction y
 *   interaction width
 *   interaction height
 *   object flags
 *   initially visible
 *   primitive array
 */

FLOPPY144_OBJECT(
    SUPPRESSION_CONTROL_PANEL,
    "SUPPRESSION CONTROL PANEL",
    FLOPPY144_SCENE_OFFICE,
    FLOPPY144_OBJECT_NONE,
    376,
    46,
    FLOPPY144_OBJECT_LAYER_FIXTURE,
    0,
    0,
    0,
    0,
    0,
    0,
    0,
    0,
    FLOPPY144_OBJECT_FLAG_NONE,
    false,
    floppy144_suppression_control_panel_primitives
)

FLOPPY144_OBJECT(
    ARCHIVE_TERMINAL,
    "ARCHIVE TERMINAL",
    FLOPPY144_SCENE_OFFICE,
    FLOPPY144_OBJECT_NONE,
    274,
    46,
    FLOPPY144_OBJECT_LAYER_FURNITURE,
    0,
    0,
    92,
    36,
    -20,
    34,
    132,
    28,
    FLOPPY144_OBJECT_FLAG_SOLID |
        FLOPPY144_OBJECT_FLAG_INTERACTABLE,
    true,
    floppy144_archive_terminal_primitives
)
